# Node
node_modules

# Env files (do not commit secrets)
.env
.env.local
.env.development
.env.production

# Prisma (generated client lives in node_modules, already ignored)
# (tidak perlu entry khusus)

# Build output
dist

# Logs
npm-debug.log*
yarn-error.log*
